name = "shomer-sos-push"
main = "sos-push-worker.js"
compatibility_date = "2024-09-01"

# FCM service-account JSON is stored as an ENCRYPTED SECRET, never in this file or the
# client. Set it after first deploy:
#   npx wrangler secret put FCM_SERVICE_ACCOUNT
# (paste the full service-account JSON from Firebase console → Project settings →
#  Service accounts → Generate new private key)
